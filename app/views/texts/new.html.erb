<div class="row">
  <div class="span4 offset6">
    <h1>Text Abuse</h1>
  </div>
</div>
<div class="row"
  <div class="span5 offset6">
      <%= form_for @text do |t| %>
        <%= t.label "Write in text to destroy" %>
        <%= t.text_area :text %>
        <div class="row">
          <div class="span5 offset7">
            <%= t.submit "Disemvowel", class: "btn" %>
            <%= t.submit "Munge", class: "btn" %>
            <%= t.submit "ROT 13", class: "btn" %>
          </div>
        </div>
      <% end %>
  </div>
</div>
